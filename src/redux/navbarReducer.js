
let initialState = {
    friends: [
        { id: 1, name: 'Sawa' },
        { id: 2, name: 'Vasya' },
        { id: 3, name: 'Petya' },
    ]
}

const navbarReducer = (state = initialState, action) => {
    return state;
}



export default navbarReducer;